# 两数之和

## 问题简介

[LeetCode 1. 两数之和](https://leetcode.cn/problems/two-sum?envType=problem-list-v2&envId=2cktkvj)

### 题目描述

给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出 **和为目标值** `target` 的那 **两个** 整数，并返回它们的数组下标。

你可以假设每种输入只会对应一个答案。但是，数组中同一个元素在答案里不能重复出现。

你可以按任意顺序返回答案。

______________________________________________________________________

## 示例说明

✅ **示例 1：**

```
输入：nums = [2,7,11,15], target = 9
输出：[0,1]
解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。
```

✅ **示例 2：**

```
输入：nums = [3,2,4], target = 6
输出：[1,2]
```

✅ **示例 3：**

```
输入：nums = [3,3], target = 6
输出：[0,1]
```

📌 **提示：**

- `2 <= nums.length <= 10⁴`
- `-10⁹ <= nums[i] <= 10⁹`
- `-10⁹ <= target <= 10⁹`
- **只会存在一个有效答案**

______________________________________________________________________

## 解题思路

### 方法一：暴力枚举（❌ 不推荐）

💡 **思路：**

- 遍历数组中的每一个元素 `nums[i]`
- 对于每个 `nums[i]`，再遍历其后的所有元素 `nums[j]`（`j > i`）
- 检查 `nums[i] + nums[j] == target` 是否成立
- 若成立，直接返回 `[i, j]`

✅ **优点：** 实现简单\
❌ **缺点：** 时间复杂度高

______________________________________________________________________

### 方法二：哈希表（✅ 推荐）

💡 **思路：**

- 利用哈希表（Map）存储 **已遍历元素的值 → 下标**
- 遍历数组，对每个元素 `nums[i]`：
  - 计算 `complement = target - nums[i]`
  - 检查 `complement` 是否已在哈希表中
    - 如果存在，说明找到了答案：`[map[complement], i]`
    - 如果不存在，将当前 `nums[i] → i` 存入哈希表
- 由于题目保证有唯一解，必能在某一步找到匹配

✅ **优点：** 时间复杂度低，只需一次遍历\
✅ **关键洞察：** “找两个数之和为 target” 等价于 “对每个数，找是否存在它的补数”

______________________________________________________________________

## 代码实现

```tabs
::: tab Java
import java.util.HashMap;
import java.util.Map;

class Solution {
    public int[] twoSum(int[] nums, int target) {
        // 哈希表：值 -> 下标
        Map<Integer, Integer> map = new HashMap<>();
        
        for (int i = 0; i < nums.length; i++) {
            int complement = target - nums[i];
            
            // 如果补数已存在，返回结果
            if (map.containsKey(complement)) {
                return new int[]{map.get(complement), i};
            }
            
            // 将当前元素存入哈希表
            map.put(nums[i], i);
        }
        
        // 题目保证有解，此处不会执行
        throw new IllegalArgumentException("No solution");
    }
}
:::

::: tab Go
func twoSum(nums []int, target int) []int {
    // 哈希表：值 -> 下标
    hash := make(map[int]int)
    
    for i, num := range nums {
        complement := target - num
        
        // 如果补数已存在，返回结果
        if j, ok := hash[complement]; ok {
            return []int{j, i}
        }
        
        // 将当前元素存入哈希表
        hash[num] = i
    }
    
    // 题目保证有解，此处不会执行
    return nil
}
:::
```

______________________________________________________________________

## 示例演示

以 `nums = [2, 7, 11, 15]`, `target = 9` 为例：

| 步骤 | i | nums[i] | complement = 9 - nums[i] | 哈希表状态 | 是否找到？ |
|------|---|---------|--------------------------|------------|-----------|
| 1 | 0 | 2 | 7 | `{}` | ❌ |
| | | | | `{2:0}` | |
| 2 | 1 | 7 | 2 | `{2:0}` | ✅ 找到！ |

→ 返回 `[0, 1]`

______________________________________________________________________

## 答案有效性证明

✅ **正确性：**

- 假设答案为 `[i, j]`（`i < j`），则 `nums[i] + nums[j] = target`
- 当遍历到 `j` 时，`i` 已被处理并存入哈希表
- 此时 `complement = target - nums[j] = nums[i]`
- 哈希表中存在 `nums[i] → i`，因此能正确返回 `[i, j]`

✅ **唯一性：**

- 题目保证“只会存在一个有效答案”，无需考虑多解情况
- 哈希表存储的是**最早出现**的下标，符合要求

✅ **无重复使用：**

- 哈希表只包含当前元素**之前**的元素，不会使用自身两次

______________________________________________________________________

## 复杂度分析

| 方法 | 时间复杂度 | 空间复杂度 |
|------------|----------------|----------------|
| 暴力枚举 | O(n²) | O(1) |
| 哈希表 | **O(n)** | **O(n)** |

- **时间：** 哈希表方法只需一次遍历，每次哈希操作平均 O(1)
- **空间：** 最坏情况下需存储 n-1 个元素（答案在最后两个）

______________________________________________________________________

## 问题总结

📌 **核心思想：**\
将“查找两个数”转化为“对每个数，查找其补数是否存在”，利用哈希表实现 O(1) 查找。

💡 **技巧提炼：**

- **空间换时间**：用哈希表避免嵌套循环
- **一次遍历即可**：边存边查，无需预处理
- **注意下标顺序**：先存的下标小，后查的下标大，天然满足 `i < j`

✅ **适用场景：**\
此类“两数之和”变种（如三数之和、四数之和）常作为更复杂问题的基础模块。

🔥 **延伸思考：**

- 如果数组有序？可用双指针（但本题无序）
- 如果要返回所有解？需调整逻辑避免重复
- 如果允许重复使用同一元素？需修改判断条件
