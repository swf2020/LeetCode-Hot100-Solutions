### 问题简介

[LeetCode 240. 搜索二维矩阵 II](https://leetcode.cn/problems/search-a-2d-matrix-ii?envType=problem-list-v2&envId=2cktkvj)

#### 题目描述

编写一个高效的算法来搜索一个 `m x n` 矩阵 `matrix` 中的一个目标值 `target`。该矩阵具有以下特性：

- 每行的元素从左到右升序排列。
- 每列的元素从上到下升序排列。

> **注意**：本题与 [74. 搜索二维矩阵](https://leetcode.cn/problems/search-a-2d-matrix/) 不同，74题中整个矩阵是严格递增的（可视为一维有序数组），而本题仅保证每行和每列分别有序。

______________________________________________________________________

### 示例说明

✅ **示例 1：**

```text
输入：
matrix = [
  [1,   4,  7, 11],
  [2,   5,  8, 12],
  [3,   6,  9, 16],
  [10, 13, 14, 17]
], target = 5

输出：true
```

✅ **示例 2：**

```text
输入：
matrix = [
  [1,   4,  7, 11],
  [2,   5,  8, 12],
  [3,   6,  9, 16],
  [10, 13, 14, 17]
], target = 20

输出：false
```

______________________________________________________________________

### 解题思路

📌 **关键观察**：\
由于每行从左到右递增，每列从上到下递增，但**不能保证全局有序**（例如右上角可能小于左下角），因此不能直接使用二分查找整个矩阵。

然而，我们可以利用“**单调性**”设计一种**类二分**的搜索策略。

______________________________________________________________________

#### ✅ 方法一：从右上角（或左下角）开始搜索（推荐）

💡 **核心思想**：\
选择一个起始点，使得在每一步都能**排除一行或一列**。

- 从**右上角** `(0, n-1)` 开始：
  - 若 `matrix[i][j] == target` → 找到，返回 `true`
  - 若 `matrix[i][j] > target` → 当前列所有下方元素都 ≥ 当前值 > target，**排除当前列**，`j--`
  - 若 `matrix[i][j] < target` → 当前行所有左侧元素都 ≤ 当前值 < target，**排除当前行**，`i++`

> 同理，也可以从**左下角** `(m-1, 0)` 开始，逻辑对称。

❌ 为什么不能从左上角或右下角？

- 左上角：右边和下边都更大，无法决定往哪走。
- 右下角：左边和上边都更小，同样无法决策。

______________________________________________________________________

#### ❌ 方法二：暴力遍历（不推荐）

- 时间复杂度 O(mn)，虽然能过，但未利用矩阵性质。

______________________________________________________________________

#### ⚠️ 方法三：对每行进行二分查找

- 因为每行有序，可对每一行做二分。
- 时间复杂度 O(m log n)
- 虽然可行，但不如方法一高效（尤其当 m 和 n 接近时）。

______________________________________________________________________

### Java 与 Go 代码实现

```java
class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {
        if (matrix == null || matrix.length == 0 || matrix[0].length == 0) {
            return false;
        }
        int m = matrix.length;
        int n = matrix[0].length;
        
        // 从右上角开始
        int i = 0, j = n - 1;
        
        while (i < m && j >= 0) {
            int val = matrix[i][j];
            if (val == target) {
                return true;
            } else if (val > target) {
                j--; // 排除当前列
            } else {
                i++; // 排除当前行
            }
        }
        return false;
    }
}
```

```go
func searchMatrix(matrix [][]int, target int) bool {
    if len(matrix) == 0 || len(matrix[0]) == 0 {
        return false
    }
    m, n := len(matrix), len(matrix[0])
    
    // 从右上角开始
    i, j := 0, n-1
    
    for i < m && j >= 0 {
        val := matrix[i][j]
        if val == target {
            return true
        } else if val > target {
            j-- // 排除当前列
        } else {
            i++ // 排除当前行
        }
    }
    return false
}
```

______________________________________________________________________

### 示例演示

以 `target = 5` 为例，矩阵如下：

```
[1,  4,  7, 11]
[2,  5,  8, 12]
[3,  6,  9, 16]
[10,13,14,17]
```

🔍 搜索路径（从右上角开始）：

| 步骤 | 当前位置 | 值 | 比较结果 | 移动方向 |
|------|----------|-----|----------------|--------|
| 1 | (0,3) | 11 | 11 > 5 | ← j-- |
| 2 | (0,2) | 7 | 7 > 5 | ← j-- |
| 3 | (0,1) | 4 | 4 < 5 | ↓ i++ |
| 4 | (1,1) | 5 | 5 == 5 ✅ | 返回 true |

______________________________________________________________________

### 答案有效性证明

✅ **正确性**：

- 每次比较后，要么排除一行，要么排除一列。
- 被排除的部分**不可能包含 target**：
  - 若 `matrix[i][j] > target`，则第 `j` 列从 `i` 到 `m-1` 都 ≥ `matrix[i][j]` > target（列有序）
  - 若 `matrix[i][j] < target`，则第 `i` 行从 `0` 到 `j` 都 ≤ `matrix[i][j]` < target（行有序）
- 因此不会漏掉可能的解。

✅ **终止性**：

- 每次循环 `i++` 或 `j--`，最多执行 `m + n` 次，必然终止。

______________________________________________________________________

### 复杂度分析

| 方法 | 时间复杂度 | 空间复杂度 | 说明 |
|------|------------|------------|------|
| 右上角搜索 | **O(m + n)** | **O(1)** | 最优解 |
| 每行二分 | O(m log n) | O(1) | 次优 |
| 暴力遍历 | O(mn) | O(1) | 不推荐 |

> 💡 当 `m ≈ n` 时，O(m + n) = O(n)，远优于 O(n log n) 或 O(n²)

______________________________________________________________________

### 问题总结

📌 **关键技巧**：\
在具有**部分有序性**的二维结构中，寻找一个“**决策点**”，使得每一步都能**单向排除**一部分搜索空间。

✅ **适用场景**：

- 行有序 + 列有序（非全局有序）
- 类似“阶梯状”单调结构

💡 **延伸思考**：

- 如果矩阵是**严格全局递增**（如 LeetCode 74），可用**虚拟一维数组 + 二分**。
- 本题的“右上角/左下角”策略是处理此类二维搜索的经典范式，值得牢记。

> 🎯 **一句话总结**：**利用行列单调性，从“拐角”出发，步步为营，线性排除！**
