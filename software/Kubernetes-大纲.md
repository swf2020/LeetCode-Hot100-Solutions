# Kubernetes 知多少 v1.0 — 目录大纲

> 版本：Kubernetes 1.32（代号 "Penelope"，2024年12月发布）
> 文档版本：v1.0
> 最后更新时间：2025-01-XX

---

## 📖 前言
- Kubernetes 的技术定位与学习价值
- 云原生时代为什么必须掌握 Kubernetes
- 本文档的阅读指南与适用人群
- 1.32 版本核心亮点速览

---

## 第一章：基础理论与架构演进

### 1.1 技术诞生背景与核心问题
- 从物理机 → 虚拟机 → 容器的演进之路
- Google Borg/Omega 的遗产：Kubernetes 的前世今生
- Kubernetes 要解决的三大核心问题：编排、调度、服务治理

### 1.2 架构演进历程
- v1.0 ~ v1.5：奠基期（基础编排能力）
- v1.6 ~ v1.12：成熟期（RBAC、CRD、CSI/CNI 插件化）
- v1.13 ~ v1.20：企业级（kubeadm GA、Sidecar 概念引入、EndpointSlice）
- v1.21 ~ v1.28：云原生深水区（Pod Security Admission、Gateway API、Sidecar Containers）
- v1.29 ~ v1.32：精细化治理（DRA、Sidecar Containers GA、结构化授权配置）
- 每次演进的驱动力分析

### 1.3 核心概念术语表
- 中英文对照 + 通俗解释（Pod、Service、Deployment、StatefulSet、DaemonSet、ConfigMap、Secret、Namespace、Node、PV/PVC、Ingress、CRD 等 20+ 术语）

### 1.4 适用场景 vs 不适用场景
- ✅ 微服务架构编排
- ✅ CI/CD 流水线基础设施
- ✅ 弹性伸缩的 Web 应用
- ✅ 批处理与大数据任务（Job/CronJob）
- ✅ 混合云/多云统一管理
- ❌ 单体小应用（运维成本 > 收益）
- ❌ 强实时性系统（如高频交易）
- ❌ 对内核有特殊依赖的裸金属场景
- ❌ 极小团队无运维能力

### 1.5 与同类技术对比
- Kubernetes vs Docker Swarm vs Apache Mesos vs HashiCorp Nomad
- 对比维度：设计哲学、架构复杂度、调度能力、生态丰富度、社区活跃度、学习曲线、生产采用率

---

## 第二章：实现原理解析

### 2.1 核心组件架构图
- 控制平面（Control Plane）：kube-apiserver、etcd、kube-scheduler、kube-controller-manager、cloud-controller-manager
- 数据平面（Data Plane）：kubelet、kube-proxy、Container Runtime（containerd/CRI-O）
- 附加组件：CoreDNS、Metrics Server、Ingress Controller
- 【Mermaid 架构图】

### 2.2 核心工作流程
- **流程一：Pod 创建全流程**（kubectl → apiserver → etcd → scheduler → kubelet → CRI）
  - 正常流程时序图
  - 异常流程：调度失败、镜像拉取失败、OOMKilled
- **流程二：Service 请求路由流程**（ClusterIP/NodePort/LoadBalancer → kube-proxy → iptables/IPVS → Pod）
  - 正常流程
  - 异常流程：Endpoint 不可用、Service 无后端
- **流程三：滚动更新流程**（Deployment → ReplicaSet → Pod 替换策略）
  - 正常流程
  - 异常流程：更新卡住、回滚操作
- **流程四：HPA 自动扩缩容流程**
  - Metrics 采集 → HPA Controller → Scale 决策 → Deployment 调整
- 【4个 Mermaid 时序图/流程图】

### 2.3 关键算法与协议详解
- **etcd 的 Raft 一致性协议**：Leader 选举、日志复制、快照机制
- **kube-scheduler 调度算法**：预选（Filtering）→ 优选（Scoring）→ 绑定（Binding），调度框架（Scheduling Framework）插件机制
- **kube-proxy 的负载均衡**：iptables 模式 vs IPVS 模式的实现差异
- **Lease 机制**：Node 心跳与 Leader Election 的实现

### 2.4 数据模型与存储结构
- etcd 中的数据组织：`/registry/<resource-type>/<namespace>/<name>` 键值结构
- API 对象的内部表示：GVK（Group/Version/Kind）与 GVR（Group/Version/Resource）
- Watch 机制与 ResourceVersion 的实现
- etcd 的 MVCC 存储引擎（BoltDB/bbolt）

### 2.5 通信协议
- kube-apiserver 的 RESTful API + gRPC 聚合
- kubelet 与 CRI 的 gRPC 通信
- etcd 客户端的 gRPC 通信
- Watch 长连接与 HTTP/2 多路复用
- Webhook（Admission/Conversion）的 HTTPS 回调

---

## 第三章：源码级实现细节

### 3.1 代码组织结构
- `cmd/`：各组件入口（kube-apiserver、kubelet、kube-scheduler 等）
- `pkg/`：核心业务逻辑
- `staging/src/k8s.io/`：独立发布的客户端库（client-go、api、apimachinery）
- `vendor/`：依赖管理
- `hack/`：构建与测试脚本
- `api/`：OpenAPI 规范
- 核心模块功能说明表

### 3.2 关键数据结构
- `Pod` 结构体（`pkg/apis/core/types.go`）
- `Informer` 与 `Lister`（client-go 的核心缓存机制）
- `workqueue`（延迟队列、限速队列、指数退避队列）
- `SharedIndexInformer` 的 Store/Indexer 实现

### 3.3 并发模型
- Go 协程（Goroutine）+ Channel 的大量应用
- Informer 的 Reflector → DeltaFIFO → Indexer 流水线
- Controller 的 worker 协程池模型
- WaitGroup 与 Context 的优雅退出

### 3.4 重要类/接口设计
- `runtime.Object` 接口（所有 API 对象的基接口）
- `Scheme`（类型注册与转换）
- `RESTStorage` 接口（API 资源的 CRUD 抽象）
- `SchedulerFramework` 接口（调度插件扩展点）
- 设计模式标注：工厂模式、策略模式、观察者模式

### 3.5 启动流程
- kube-apiserver 启动全过程：命令行解析 → 配置构建 → 链式 Handler 注册 → HTTPS 服务启动
- kubelet 启动全过程：配置加载 → PLEG 启动 → syncLoop 主循环
- kube-scheduler 启动全过程：调度框架初始化 → Informer 同步 → 调度循环

---

## 第四章：设计模式深度剖析

### 4.1 架构层面的模式
- **声明式 API + 控制器模式（Reconciliation Loop）**：Kubernetes 的灵魂设计
- **Operator 模式**：将运维知识编码为控制器
- **Sidecar 模式**：1.32 中 Sidecar Containers 正式 GA 的意义
- **Ambassador / Adapter 模式**：在 Pod 多容器设计中的应用

### 4.2 GoF 设计模式在源码中的应用
- **工厂模式**：`cmdutil.Factory` 接口（kubectl 命令构建）
- **策略模式**：调度器的 Score 插件（不同评分策略可插拔）
- **观察者模式**：Informer 的 EventHandler 回调机制
- **建造者模式**：`PodBuilder` 在测试代码中的应用
- **责任链模式**：Admission Webhook 链式处理
- 每个模式标注源码位置

### 4.3 分布式系统模式
- **Leader Election**：基于 Lease 对象的选主实现
- **Watch + Cache**：List-Watch 机制避免 apiserver 过载
- **Optimistic Concurrency**：ResourceVersion 实现乐观锁
- **Level-Triggered（电平触发）vs Edge-Triggered（边沿触发）**：为什么 K8s 选择电平触发

### 4.4 设计权衡分析
- 声明式 vs 命令式：为什么选择声明式
- 最终一致性 vs 强一致性：控制器的收敛保证
- 简单性 vs 灵活性：CRD + Operator 的扩展哲学

---

## 第五章：高可靠与高可用方案

### 5.1 集群部署架构
- 单节点开发环境（Minikube/Kind）
- 三节点高可用控制平面（Stacked etcd）
- 外置 etcd 集群的高可用架构
- 超大规模集群（5000+ 节点）的架构考量
- 【Mermaid 部署架构图】

### 5.2 数据一致性保证
- etcd 的 Raft 强一致性
- apiserver 缓存的最终一致性
- ResourceVersion 与 Watch Bookmark 机制
- 多 apiserver 实例的一致性保证

### 5.3 故障检测与恢复
- Node 故障检测：NodeLease + NodeStatus 双重机制
- Pod 健康检查：Liveness / Readiness / Startup Probe
- 控制平面故障恢复：etcd 备份恢复、apiserver 无状态重启
- 超时时间设置建议（node-monitor-grace-period、pod-eviction-timeout 等）

### 5.4 容灾策略
- 多 AZ 部署：Pod 拓扑分布约束（TopologySpreadConstraints）
- 跨地域集群联邦（KubeFed）
- etcd 跨 AZ 部署的网络延迟考量
- Velero 备份与灾难恢复

### 5.5 性能优化（30+ 调优参数）
- **apiserver 调优**：--max-requests-inflight、--max-mutating-requests-inflight、--watch-cache-sizes
- **etcd 调优**：--quota-backend-bytes、--snapshot-count、--heartbeat-interval
- **kubelet 调优**：--max-pods、--kube-api-qps、--kube-api-burst、--serialize-image-pulls
- **scheduler 调优**：--percentageOfNodesToScore、调度器并行度
- **kube-proxy 调优**：IPVS 模式、--ipvs-min-sync-period
- **集群级调优**：API Priority and Fairness（APF）、ResourceQuota、LimitRange
- **网络调优**：CNI 选型、MTU 优化、conntrack 表大小
- **存储调优**：CSI 驱动参数、PV 回收策略
- 完整参数表格（参数名、默认值、推荐值、说明）

### 5.6 可观测性
- **Metrics**：kube-apiserver、kubelet、etcd、scheduler 的关键 Prometheus 指标
- **健康检查接口**：/healthz、/livez、/readyz 端点
- **日志规范**：结构化日志（klog v2）、日志级别设置
- **分布式追踪**：OpenTelemetry 集成（1.32 中的追踪增强）
- **审计日志**：Audit Policy 配置

---

## 第六章：实战指南与问题排查

### 6.1 最佳实践
- 资源请求与限制（Requests/Limits）配置规范
- Namespace 隔离策略
- RBAC 权限最小化原则
- 镜像管理（标签规范、拉取策略）
- Pod 安全标准（Pod Security Standards）
- 网络策略（NetworkPolicy）配置

### 6.2 性能压测
- 推荐工具：kube-burner、ClusterLoader2、k6
- 典型压测场景与指标解读
- API 延迟、Pod 启动延迟、调度延迟基准值

### 6.3 常见故障模式（12个）
1. Pod 一直 Pending — 调度失败排查
2. Pod CrashLoopBackOff — 容器反复崩溃
3. ImagePullBackOff — 镜像拉取失败
4. Node NotReady — 节点不可用
5. Service 无法访问后端 Pod
6. PVC Pending — 存储卷绑定失败
7. DNS 解析失败
8. OOMKilled — 内存溢出
9. Evicted Pod — 节点资源压力驱逐
10. RBAC 权限不足（403 Forbidden）
11. etcd 性能劣化导致 apiserver 超时
12. 证书过期导致集群通信中断

每个故障包含：现象描述、排查命令、根因分析、解决方案、预防措施

### 6.4 故障演练（Chaos Engineering）
- 使用 Chaos Mesh / LitmusChaos 的场景设计
- Pod 故障注入
- 网络分区模拟
- 节点故障模拟
- etcd Leader 切换测试

---

## 第七章：面试题库与学习路径

### 7.1 高频面试题
- **基础题**（10题）：Pod 生命周期、Service 类型、Deployment 与 StatefulSet 区别等
- **进阶题**（10题）：Informer 机制、调度器扩展、CRD 开发等
- **原理题**（10题）：etcd Raft、apiserver 链式处理、GC 机制等
- **场景题**（5题）：生产故障排查、架构设计、性能优化等
- 每题附详细答案

### 7.2 学习资源
- 官方文档必读章节
- 经典书籍推荐
- 核心论文（Borg、Omega、Kubernetes 设计论文）
- 优质博客与视频

### 7.3 实验环境搭建
- Kind 快速搭建多节点集群（附完整配置）
- Minikube 单节点开发环境
- kubeadm 生产级集群搭建要点

---

## 附录

### 附录A：核心术语表
- 50+ 术语中英文对照与解释

### 附录B：参考文献
- 官方文档链接
- 源码仓库
- KEP（Kubernetes Enhancement Proposals）索引
- 相关论文

### 附录C：Kubernetes 1.32 版本更新记录
- GA 特性列表
- Beta 特性列表
- 废弃与移除项

---

> 本文档最后更新时间：2025-01-XX
> 基于 Kubernetes 1.32 "Penelope" 版本
